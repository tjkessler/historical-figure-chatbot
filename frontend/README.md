# README for Frontend

This folder contains the React + TypeScript frontend for the Historical Figure Chatbot.

## Setup

1. Install dependencies:
   npm install
2. Start the development server:
   npm start

The app will run on http://localhost:3000
